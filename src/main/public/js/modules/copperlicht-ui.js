/**
 * copperlicht-ui.js
 *
 * Contains the basic user-interaction elements
 * that copperlicht uses to update its display.
 */

$(function () {
    // Begin by binding the keypress event to the canvas object
    $("body").keypress(function (event) {
        
    });
});