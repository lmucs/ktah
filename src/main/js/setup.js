/**
 * setup.js
 * 
 * Contains the single global object, ktah, which contains
 * all other game components.
 * 
 * !!! Include this script before all others !!!
 *
 */

// Global ktah object
var ktah = {
    
    // Container object to hold the types
    types: {}
    
};
